<div>
    <h1>Food & Beverages - {{products.length}} items</h1>

    <div>
        {{#each products}}
        <div style="background: url('{{this.photo}}');">
            <a href="/product/{{this._id}}">
                {{this.name}}
            </a>
        </div>
    </div>
    {{/each}}
</div>