<div>
        {{#each product}}
        <img style="height: 40px;" src="{{this.photo}}">
        <p>{{this.name}}</p>
        <a href="product/{{this.id}}">View product details</a>
        <p>${{this.price}}</p>
        {{/each}}

        {{#each order}}
        <p>{{this.quantity}}</p>
        {{/each}}

        
    
    </div>
    
    <div class="orderSummary">
    <h2>Order Summary</h2>
    {{#each invoice}}
    <ul>
        <li>{{this.order}}</li>
    </ul>
    {{/each}}
    
    </div>